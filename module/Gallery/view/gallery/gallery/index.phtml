<?php

/**
 * @var $slugs
 */

$title = 'Index gallery page';
$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>

<?php foreach ($slugs as $slug) : ?>
<span>
    <a href="<?= $this->url('gallery', ['action' => 'arts', 'slug' => $slug->getSlug()]) ?>">
        <?= $slug->getText() ?>
    </a>
</span>
<?php endforeach; ?>
